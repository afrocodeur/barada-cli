
export default class Auth {

    handle() {
        // Check if the user is logged
    }

    redirectTo() {
        // Redirect the user to
    }

    component() {
        // display that component instead of the page
    }
}