import React, { Component } from 'react';
import Router from 'barada/helpers/Router';


// Router.add('/', Component).name('home')
// Router.add('/', function(){ return <h2>Exemple</h2> }).name('exemple')